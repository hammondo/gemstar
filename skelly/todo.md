# Todo

1. In the first step of the new campaign wizard, show only the list of search terms used to build the prompt, not the entire prompt.  Allow the user to add/remove/modify this list before moving on, with the option to save these as the new defaults (in monitor-search-terms-terms.json), or use just this time.

2. On the same page, show the current brief so the user knows whether they need to update it or continue with current brief.

3. On the same page, add an option for ai to pre-research and suggest search terms before the more in-depth research task.

4. On the settings page, place api health and cron schedules side-by-side rather than full width

